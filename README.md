1.
После прочтения задачи я решил выполнить все с нуля
Для выполнения я выбрал Python3, так как я с ним больше знаком, чем с Node.js
Я поднял чистый Rocky в Hyper-V (Пресет Server with GUI), потому что я чаще взаимодействую с RHEL системами, чем Дебиан
Поставил репозитории Docker и Nginx, установил все необходимые пакеты, среди них
1. Docker Engine
2. Docker Compose
3. Nginx
4. Python3

2.
Первым делом я написал логику сервера
В задаче указано поднять простой сервер
Для этого я использую http.server и обработчик запросов
Почему Threading, потому что http.server последовательный, если кто-то в дороге зависнет и не получит ответ, то другие клиенты будут ждать
urlparse тут для более корректного поведения, чтобы запросы попадали в нужный путь
Далее я написал свой класс, так как BaseHTTPRequestHandler может читать запросы и отправлять ответы, но не знает конкретной логики для нашей задачи

# проверить версию python в Dockerfile
# проверить путь /app
# уточнить механику работы и про многостадийную сборку

# описать dockerignore

Создал nginx.conf для базового реверс прокси
backend - название сервиса из compose.yml, Compose создает днс внутри себя и мы резолвим имя backend
http://backend_upstream - nginx проксирует запросы на backend
host для сохранения исходного хоста

Compose:

Собираем образ из директории ./back
Expose: Не публикуем порт и открываем его для контейнеров в Compose
не используем 
ports:
	- "8080:8080"
Так как бэкенд не должен быть доступен
volumes: Подменям конфиг nginx своим конфигом
